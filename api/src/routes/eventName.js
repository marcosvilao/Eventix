const {Router} = require("express");


const route = Router();

route.get("/", async(req,res)=>{

    try {
        
        const {name} = req.body;



    } catch (error) {
        
    }

});

module.exports = route;